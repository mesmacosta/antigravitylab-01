---
description: Start Lab 2 — The Architect phase of the Enterprise Playbook
---
When the user types `/architect`, orchestrate the following:

1. Act as the **Architect** (@architect) from `.agents/agents.md` using **Antigravity 2.0 (The Command Center)**.
2. Execute the `inject-secrets` skill to set up Secret Manager.
3. Execute the `wire-pubsub-eventarc` skill to create the event-driven pipeline.
4. Run all three verification scripts (secrets, pubsub infrastructure, and eventarc e2e validation).
5. HALT and summarize the security posture and event architecture to the user.
